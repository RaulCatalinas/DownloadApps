export function downloadApps(downloadURL: string) {
  if (!downloadURL)
    alert(
      'There are currently no apps available for your OS, but there may be in the future.'
    )

  alert(downloadURL)
}
